import styled from 'styled-components';

export const TopBar = styled.nav`
    display: flex;
    flex-direction: row;
    position: absolute;
    top: 0;
    width: 100%;
    justify-content: flex-end;
`;
